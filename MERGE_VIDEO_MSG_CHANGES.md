# 视频解析合并转发功能修改说明

## 修改概述
本次修改为 R插件 添加了视频解析消息合并转发功能，将原本分散发送的视频信息、视频链接和评论区图片合并成一条"合并聊天记录"发送，有效减少群内刷屏。

## 修改的文件

### 1. `config/tools.yaml`
添加了两个新的配置项：
- `biliMergeVideoMsg`: 哔哩哔哩是否合并转发视频解析消息（默认：true）
- `douyinMergeVideoMsg`: 抖音是否合并转发视频解析消息（默认：true）

### 2. `apps/tools.js`
#### 2.1 构造函数中添加配置加载
- 第 366 行：添加 `this.biliMergeVideoMsg` 配置加载
- 第 428 行：添加 `this.douyinMergeVideoMsg` 配置加载

#### 2.2 修改 `biliComment` 方法（约第 1319 行）
- 添加 `returnData` 参数，支持返回评论数据而不是直接发送
- 当 `returnData=true` 时，返回评论图片数组或转发消息格式数据
- 保持向后兼容，默认行为不变

#### 2.3 修改 `douyinComment` 方法（约第 1263 行）
- 添加 `returnData` 参数，支持返回评论数据而不是直接发送
- 当 `returnData=true` 时，返回评论图片数组或转发消息格式数据
- 保持向后兼容，默认行为不变

#### 2.4 修改 B站视频解析逻辑（`bili` 方法，约第 1726 行）
- 根据 `biliMergeVideoMsg` 配置判断是否使用合并转发
- **合并转发模式**：
  1. 不立即发送视频信息
  2. 下载并发送视频
  3. 收集视频信息、视频链接、评论数据到 `forwardMsg` 数组
  4. 使用 `Bot.makeForwardMsg()` 构造并发送合并消息
- **传统模式**：
  1. 先发送视频信息
  2. 下载并发送视频
  3. 单独发送评论

#### 2.5 修改抖音视频解析逻辑（`handleDouyinResolvedVideo` 方法，约第 853 行）
- 根据 `douyinMergeVideoMsg` 配置判断是否使用合并转发
- **合并转发模式**：
  1. 收集视频信息（含封面）
  2. 下载并发送视频
  3. 获取评论数据并添加到 `forwardMsg` 数组
  4. 使用 `Bot.makeForwardMsg()` 构造并发送合并消息
- **传统模式**：保持原有的分别发送逻辑

### 3. `guoba.support.js`
#### 3.1 哔哩哔哩配置区（约第 464 行后）
添加配置项：
```javascript
{
    field: "tools.biliMergeVideoMsg",
    label: "合并转发视频解析消息",
    bottomHelpMessage: "开启后，将视频信息、链接和评论区图片打包成合并聊天记录发送，防止刷屏（默认开启）",
    component: "Switch",
    required: false,
}
```

#### 3.2 抖音配置区（约第 558 行后）
添加配置项：
```javascript
{
    field: "tools.douyinMergeVideoMsg",
    label: "合并转发视频解析消息",
    bottomHelpMessage: "开启后，将视频信息、链接和评论区图片打包成合并聊天记录发送，防止刷屏（默认开启）",
    component: "Switch",
    required: false,
}
```

## 功能说明

### 合并转发消息结构
当开启合并转发后，消息结构如下：

#### 哔哩哔哩
1. **视频信息**：包含封面、标题、UP主、播放数据等
2. **视频链接**：原始视频URL
3. **评论截图**：如果开启了评论功能，会包含评论区截图

#### 抖音
1. **视频信息**：包含封面、作者、简介等
2. **视频文件**：实际的视频（仍然单独发送）
3. **评论截图**：如果开启了评论功能，会包含评论区截图

### 配置方式

#### 通过 Guoba 面板配置（推荐）
1. 进入 Guoba 插件配置界面
2. 找到"哔哩哔哩配置"或"抖音配置"分组
3. 找到"合并转发视频解析消息"开关
4. 根据需要开启或关闭

#### 通过配置文件配置
编辑 `config/tools.yaml`：
```yaml
biliMergeVideoMsg: true  # 哔哩哔哩合并转发（true=开启，false=关闭）
douyinMergeVideoMsg: true  # 抖音合并转发（true=开启，false=关闭）
```

## 向后兼容性
- 默认开启合并转发功能
- 关闭后完全恢复为原有的分散发送模式
- 不影响其他平台的视频解析
- 评论功能的开关（`biliComments`/`douyinComments`）仍然独立控制

## 注意事项
1. 合并转发模式下，视频信息和评论会打包在一条转发消息中，需要点开查看
2. 视频文件本身仍然单独发送（因为合并转发不支持直接包含视频文件）
3. 如果未开启评论功能，则只合并视频信息和视频链接
4. 某些平台可能没有评论区图片，会自动省略该部分

## 测试建议
1. 测试哔哩哔哩视频解析（开启/关闭合并转发）
2. 测试抖音视频解析（开启/关闭合并转发）
3. 测试评论功能开启/关闭时的表现
4. 测试无评论的视频
5. 验证 Guoba 配置界面是否正常工作

## 开发者信息
- 修改日期：2026-07-10
- 修改目的：减少群内刷屏，优化用户体验
